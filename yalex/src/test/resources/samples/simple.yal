(* Comentario inicial *)

{
from tokens import PLUS, NUM
}

let digit = ['0'-'9']

rule gettoken =
    [' ' '\t' '\n'] { return lexbuf }
  | digit+            { return NUM }
  | '+'               { return PLUS }
  | eof               { raise Exception('fin') }

{
def helper():
    return 1
}
